# 质量闭环：验证矩阵与修复日志

## 工作边界

- 隔离工作树：`F:\program\Aopen-pro\write\.worktrees\story-audit-quality-20260908`。
- 分支：`codex/quality-closure-20260908`；基线：`c7e11111f929ffe2f29c2c1144d002a56b58ebf1`。
- 修复范围为 `scripts/`、`tests/` 和本文档；主代理负责集成、README、SKILL 及最终提交。
- 保持纯 Python API、Python 3.8+ 标准库、零运行依赖契约。
- 复现、写入实验和 API 冒烟均在 `TemporaryDirectory` 构造项目；不操作仓库中的小说正文或设定。
- 审查代理统一问题编号并独立复验。修复侧完成后只记录“待复验”，不自行认定最终关闭。

## 已执行的准备验证

- `git status --short --branch`：初始工作树干净，分支与指定分支一致。
- `python -m pytest --collect-only -qq`：成功收集 16 个模块、252 项测试。
- 阅读 README、公开 API 签名、核心执行管线和测试初始化方式；通过 `ast` 生成测试结构大纲。
- 主代理提供的基线结果：`python -m pytest -q --cov=scripts --cov-report=term-missing` 得到 `252 passed, 12 subtests passed`，覆盖率 89%。修复侧不重复运行此基线。
- 现有测试主体是 `unittest.TestCase`，文件写入使用 `TemporaryDirectory`；历史问题回归模块采用 pytest 的 `tmp_path`、`capsys`。未发现共享 `conftest.py`、pytest 配置文件或运行依赖清单。
- 辅助脚本使用 `python -X utf8` 输出中文；AST 读取用 `utf-8-sig` 兼容现有 BOM 文件。

## 验证矩阵

| 验证面 | 现有模块与数量 | 本轮风险与必要断言 | 修复后的定向验证 |
| --- | --- | --- | --- |
| 公开 API 与报告 | `test_story_audit_api.py` 23；`test_story_audit_upgrades.py` 4；`test_audit_bugs_and_enhancements.py` 23 | 状态码 0/1/2/3；失败路径不得作为本轮报告返回；缺字段或类型错误不得写正文；静默约定 | 新增对应失败回归，再跑相关 API/集成模块 |
| 编码、原子写入与补丁 | `test_safe_io.py` 20；`test_safe_writer.py` 12 | UTF-8/BOM/GB18030 与 LF/CRLF 保真；歧义或锚点不匹配应拒绝；失败不得改变正文；备份可还原 | 回归测试断言原始字节、目录副作用、备份内容与状态码 |
| 资源账本 | `test_ledger_engine.py` 35 | JSON/Markdown 双轨一致；冷资产与状态流转保护；脏写拦截；写入失败不覆盖可信状态 | 模型与存储定向测试，必要时注入确定性文件系统故障 |
| 章节与接缝 | `test_chapter_resolver.py` 33；`test_chapter_linker.py` 23 | 自然排序、小数章、重复章和缺章语义；允许部分范围；边界与隔离区定位 | 用临时章文件验证真实发现结果及报告，不只模拟返回值 |
| 格式、AI、题材与平台 | `test_format_scanner.py` 43；`test_ai_patterns_checker.py` 7；`test_genre_detector.py` 7；`test_platform_rubrics.py` 3 | 掩码与原文行号对应；模式命中边界；题材与平台有效值；严重度和报告一致 | 只为确认的真实误判补回归，保持既有规则约定 |
| 跨批状态、偏好与运行时 | `test_audit_state.py` 2；`test_author_memory.py` 5；`test_runtime_detector.py` 3；`test_types.py` 9 | 完成章、开放缺陷和伏笔不丢失；失败不冒充完成；记忆字节限制；子代理递归降级 | 先真实写盘/重读，再验证跨次 API 调用的状态一致性 |
| 批量复杂度 | 复用 API 与章节发现测试 | 批量 N 章当前可能重复发现 N+1 次；保留全部归档、顺序与错误行为 | 以发现次数和报告内容作稳定断言，不把耗时阈值写入测试 |
| 集成与兼容性 | 全部 `tests/` 和 `scripts/` | 无意外文件、无新增运行依赖、Python 3.8 语法兼容 | 主代理最终全量 pytest、编译/兼容性检查和 diff 审阅 |

## 修复顺序与交接模板

1. 优先处理会误写正文或破坏持久化数据的问题。
2. 处理失败传播、跨批状态和审查完成标记问题。
3. 处理参数、报告路径及其他对外契约问题。
4. 最后处理已量化的批量重复扫描，避免与正确性修复混杂。

每个问题按以下顺序记录，不凭猜测修改生产代码：

| 字段 | 内容要求 |
| --- | --- |
| 编号 / 严重程度 | 审查代理确认的 Rxx 与等级 |
| 定位 / 根因 | 文件、行号和输入如何触发错误 |
| 修复计划 | 最小变更及保留的公开契约 |
| RED 证据 | 新增回归测试、实际命令、预期失败和实际失败 |
| 变更 | 生产代码与测试的具体调整 |
| GREEN 证据 | 定向测试命令、通过数量、必要的字节或状态验证 |
| 待复验状态 | 通知审查代理的时间点与待验证事项；独立复验结果另行记录 |

## 准备阶段实证（待审查代理编号）

以下实验均在临时项目中执行，尚未修改生产代码：

- 补丁字典缺少 `new_text`：返回 0，旧正文被替换为空串；`new_text=None`：返回 0，正文变成字面 `None`。`target_line='bad'` 向调用者泄漏 `ValueError`；`target_line=2.9` 被截断成 2。位置：`scripts/story_audit.py:1655` 附近的默认值和强制转换。
- 批量报告陈旧引用：首次 `audit_scope(scope_str='1-1')` 成功，删除临时章节后重复调用返回状态码 3，却仍返回首轮已存在的汇总报告路径。位置：`scripts/story_audit.py:1541` 附近只检查路径存在性。
- 主代理提供的重复扫描证据：30/100/200 章分别调用章节发现 31/101/201 次；200 章总耗时 16.2728 秒，其中章节发现耗时 14.4409 秒。等待统一编号后用稳定的调用次数回归验证修复。

## 修复记录

### R06 / P1：非法补丁被强制转换后写坏正文

- 定位 / 根因：`story_audit.apply_fix` 为字典缺字段提供空串默认值，并对字段调用 `int` / `str`；`run_apply_fix` 对文件和显式参数的验证不一致，部分非法值在生成备份后才失败。
- 修复计划：保留输入原类型，在共同的回写管线校验必填字段、正整数行号与字符串字段；非法输入统一返回 3，写入前拒绝；保留显式 `new_text=''` 和正常 `PatchSpec` 的行为。
- RED 测试：`tests/test_quality_closure.py` 覆盖字典/JSON 文件缺字段、四种入口的类型与行号错误、显式删除、多行替换以及原始字节备份。
- RED 命令：`python -X utf8 -m pytest -q tests/test_quality_closure.py -k r06 --tb=short`，实际结果 `33 failed, 17 passed`；失败包括误返回 0、`ValueError` 外泄和非法新文本触发备份目录副作用，符合原始缺陷。
- 变更：移除字典与 JSON 文件的 `int` / `str` 强转，缺字段保持 `None`；共同回写管线在生成备份前验证正整数行号和文本类型，保持显式空串删除。
- GREEN：`python -X utf8 -m pytest -q tests/test_quality_closure.py tests/test_safe_writer.py tests/test_story_audit_api.py --tb=short`，实际 `85 passed`；包含本轮 50 项补丁参数/无副作用/合法写入回归及原有 35 项补丁/API 测试。
- 独立复验：审查代理执行本轮 50 项回归全部通过；另用 44 组独立临时实验核验非法输入无副作用，以及 UTF-8 BOM/GB18030 CRLF 下显式删除、多行替换和原字节备份，全部通过。
- 状态：审查代理已针对性关闭 R06，最终全量集成回归待全部问题修复后执行。

### R01 / P1：损坏持久化状态被当作空状态覆盖

- 定位 / 根因：`load_ledger_state`、`sync_from_markdown`、`load_audit_state` 吞掉解析或类型错误后初始化空对象；`LedgerState.from_dict` 跳过非对象资产；作者记忆把错误 `preferences` 自动替换为空字典。
- 修复计划：仅在状态文件不存在时使用初始状态；已有 JSON 或字段结构损坏必须抛出明确错误，由公开执行管线映射到 3；在首次持久化前读取并验证所有需要修改的状态，不写出误导成功报告。保留合法初始空对象与缺省可选字段。
- RED 测试：同一真实坏账本分别调用单章、建账、同步与封账 API；坏审计状态调用单章/批量；坏作者偏好调用 `record`。断言所有已有文件字节及目录树保持不变。
- RED：`python -X utf8 -m pytest -q tests/test_quality_closure.py -k r01 --tb=short` 实际 `30 failed`。除假成功和原数据覆盖外，还复现了损坏列表在写 JSON 后才因 `.get` 崩溃。
- 变更：停止吞掉加载异常；账本与审计状态反序列化检查容器及条目结构；作者记忆拒绝损坏偏好；单章管线将审计状态的读取提前到第一次写入之前，建账/封账/同步/批量边界将错误映射为状态码 3。
- 验证中的契约调整：首轮相关模块结果 `171 passed, 1 failed`，旧测试 `test_sync_from_markdown_resilience` 明确要求损坏 JSON 和非法数量被默认为新账本，正好固化了 R01/R09 的数据覆盖缺陷。将其更新为抛错且双文件字节不变；合法删除整行的原有测试保留。
- GREEN：修复及原有相关模块合计 `172 passed`。补充“仅旧 Markdown 存在”的回归先得到 `1 failed`，增加保护后继续定向复验。
- 补充保护：默认建账遇到已有 Markdown 但缺少 JSON 时返回 3，提示显式同步，防止把旧资产当作不存在；`force=True` 保留原有明确覆盖语义。
- 独立复验重开：审查代理证明只校验容器仍不够；坏 `open_defects.chapter`、字符串资产数量、数值偏好内容会在写报告/JSON 后才报错。
- 第二轮 RED：新增字段级回归及 R03 保存竞态回归，实际 `13 failed`；包含上述三类输入及非有限数量、坏资产历史/约束、坏偏好键等邻近字段。
- 第二轮变更：在反序列化边界验证后续运算所需的有限数值、字符串和嵌套容器，保留可选字段缺省值，不通过末端吞错修补。
- 第二轮 GREEN：`python -X utf8 -m pytest -q tests/test_quality_closure.py -k 'r01 or r03' --tb=short`，`46 passed`（R01 43 项、R03 3 项）。
- 状态：字段级补全已送审查代理再次复验。

### R02 / P1：批量忽略失败状态并误用空汇总

- 定位 / 根因：`run_scope_audit` 对单章返回 3 仍追加空字典，后续读取 `word_count` 触发 `KeyError`；完成章与开放问题更新也缺少失败分支。
- 修复计划 / 变更：在首个失败章立即返回 3，保留此前成功章的单章归档；本次批量不更新完成状态，不覆盖旧批量汇总，也不把失败章记为完成。
- RED：`python -X utf8 -m pytest -q tests/test_quality_closure.py -k 'r02 or r03 or r07' --tb=short` 中 R02 两例均因 `KeyError('word_count')` 失败，覆盖坏章节与脏账本。
- GREEN：R02/R03/R07 与原有 API、升级测试合计 `113 passed`。
- 独立复验 / 状态：审查代理验证坏章处于首/中/末三种位置均返回 3 和空路径，原最新报告、同名批报告、审计状态逐字节保留；R02 针对性关闭，最终集成回归待执行。

### R03 / P1：建账吞掉扫描错误后写出假成功

- 定位 / 根因：建账扫描循环捕获所有异常后继续，使用目标列表尾章作为进度并提交不完整状态。
- 修复计划 / 变更：任一章节读取或提取失败就返回 `(3, Path(''))`，尚在内存中的资产不落盘；合法空白章节仍按原规则处理。
- RED：上述批次中 R03 两例实际失败，分别覆盖全新项目及已有账本；坏第二章却返回成功报告并更新进度。
- 首轮 GREEN：R02/R03/R07 与原有 API、升级测试合计 `113 passed`；审查代理另验证坏章处于首/中/末均不提交账本。
- 独立复验重开：保存阶段的 `LedgerDirtyError` 仍返回裸 3 并输出错误，违反返回形状与 `silent`。新增确定性保存竞态回归先失败，修复为 `(3, Path(''))` 且只在非静默模式输出。
- 第二轮 GREEN：与 R01 字段级回归合计 `46 passed`；R03 3 项全部通过。
- 状态：返回形状补全已送审查代理再次复验。

### R07 / P2：错误调用仍返回旧范围报告

- 定位 / 根因：`audit_scope` 依据固定范围文件存在性返回路径，没有判断本次是否失败。
- 修复计划 / 变更：底层返回 3 时直接返回空路径，不复用旧汇总。与 R02 同批修复，避免批量错误路径仍冒充本轮产物。
- RED：上述批次中 R07 一例实际失败：第一次范围审查成功、删除临时章节后再次调用，状态码虽为 3 仍返回第一次报告。
- GREEN：R02/R03/R07 与原有 API、升级测试合计 `113 passed`。
- 独立复验 / 状态：审查代理的首/中/末坏章与旧报告字节验证均通过；R07 针对性关闭，最终集成回归待执行。

### R08 / P1：Markdown 单列修改被旧变更检测遗漏

- 定位 / 根因：`sync_from_markdown` 只比较部分字段，单独修改 category/unit/owner/origin_chapter 时不更新 JSON，随后重渲染把作者修改抹掉。
- 修复计划 / 变更：把这四个字段纳入已有变更检测与赋值，继续保留历史流水和借出元数据，不新增同步格式。
- RED：四种单列修改分别失败，JSON 仍是旧值。
- GREEN：R08/R09 共 19 项回归通过；原账本、API 与历史问题模块共 `81 passed`。
- 独立复验 / 状态：审查代理独立验证四个单列修改与 history/lend_meta 保留通过，R08 针对性关闭。

### R09 / P1：错误表格行被跳过后误作删除

- 定位 / 根因：资产表解析跳过短行/坏值，随后使用不完整 ID 集合删除资产；非法数量、章节、类别和状态被默认值替代。
- 修复计划 / 变更：按资产表上下文解析；在保存前检查必需表头、列数、行分隔符、唯一非空 ID、有限数值及合法类别/状态。任何坏行使本次同步返回 3，原 JSON 与编辑中的 Markdown 均保留；正确的整行删除和冷资产保护继续沿用原契约。
- RED：与 R08 首批合计 `16 failed`。补充零列行、错误约束和大整数精度回归又得到 `3 failed`，分别证实跳行、丢弃约束和 float 转换损失整数精度。
- 变更补充：数量先按整数解析，避免丢失 9007199254740993 的精度；错误约束不能静默丢弃，保留原有 `-`、`无` 空约束写法。
- GREEN：`python -X utf8 -m pytest -q tests/test_quality_closure.py -k 'r08 or r09' --tb=short` 得到 `19 passed`；原账本、API 与历史问题模块 `81 passed`，其中明确删除资产、冷资产保护和缺失 JSON 时从有效 Markdown 恢复等场景继续通过。
- 第二轮重开：审查代理发现第一张热资产表的表头缺少前导 `|` 时整张表被跳过，后续合法冷表仍使同步删除热资产。扩展到冷热两表的缺前导分隔符、删除表头、数据行前插空行，共 6 项新增回归全部 RED。
- 第二轮变更：在跳过非表格行前识别明显资产表头，并保留冷热资产区域上下文；区域内的数据行没有有效表头即拒绝，不能靠另一张合法表启动部分同步。
- 第二轮 GREEN：新旧 R08/R09 与原有同步正例共 `29 passed`，原字节保持断言通过。一次命令误引用不存在的 `tests/test_upgrade_features.py` 未执行测试，随后改用实际存在模块完成上述验证。
- 第二轮独立复验 / 状态：审查代理定向 `21 passed`，并独立验证冷热两表各 3 种坏表格、原重现场景与合法删除/隐藏冷区块/仅冷资产/空账本正例；R09 针对性关闭，首轮重开记录保留。

### R11 / R15 与 R14 扫描器部分（主代理修复）

- R11 根因及变更：裁剪句子后仍使用旧 start/end，导致闭引号加空格触发越界；主代理统一裁剪内容与绝对偏移，并保留引号内外逗号降噪规则。
- R15 根因及变更：尾窗只按行起点过滤，漏掉长段落末尾命中；改按候选命中的绝对位置判断并保留原始行号与片段。
- R14 扫描器部分：未闭合引号不再逐个重扫后缀，使用与原规则等价的线性对话区间扫描。
- RED / GREEN：主代理新增 `tests/test_scanner_quality.py` 29 项，先 `17 failed, 12 passed`；修复后新旧扫描器共 `79 passed`（0.41 秒）。主代理另验证 21845 个短输入等价；审查代理另验证 1500 个随机输入及 4 次单章、1 次批量 API。
- 性能证据：4000 个未闭合引号的掩码耗时从 0.250720 秒降至 0.001244 秒；40000 字符完整扫描主代理测 0.035520 秒、审查代理测 0.034700 秒。
- 状态：审查代理针对性关闭 R11、R15；R14 扫描器侧通过，账本 `【` 分支仍待后续修复。

### R12 / P2：默认持有人未继承所有者

- 定位 / 根因：`AssetItem.current_holder` 默认固定为“主角”，使 `__post_init__` 的所有者回填逻辑无法生效。
- 修复计划 / 变更：默认值改为空字符串，使用原有回填逻辑继承 owner；明确传入不同持有人仍原样保留。
- RED：两项回归得到 `1 failed, 1 passed`，默认持有人仍是“主角”；明确借用人场景原先即正确。
- GREEN：两项新回归及原模型测试 `10 passed`；随后与 R14 新回归和原账本模块合计 `42 passed`。
- 独立复验 / 状态：审查代理的 5 个独立持有人场景通过，R12 针对性关闭。

### R14 / P2：账本未闭合括号引发重复扫描

- 定位 / 根因：`HEURISTIC_BRACKET_PATTERN.finditer` 对每个左括号重扫后缀；没有可匹配提示的长块也会重复尝试。
- 修复计划 / 变更：按右括号划分候选区间，每段只从首个左括号尝试一次原有正则；缺失右括号时停止该扫描，不截断完整文本或删除合法后缀。
- RED：4 项正常/嵌套/未闭合语义对照通过；60000 个无效左括号加合法后缀的独立进程超过宽裕的 5 秒预算，回归失败。
- GREEN：本轮 5 项回归与 R12、原账本模块合计 `42 passed`；独立比对 6000 组随机短输入的匹配区间与命名捕获组，全部与原正则相同。
- 性能证据：4000 个无效前缀加合法后缀用时 0.000348 秒；60000 个用时 0.004981 秒，灵石 3 枚的合法后缀仍完整提取。
- 独立复验：审查代理另比较 2505 个短样本的 span/groupdict 与完整资产候选均与基线一致；60000 个未闭合左括号耗时 0.002258 秒，加合法后缀为 0.005372 秒且保留灵石 3 枚。主代理对 1000/2000/4000/60000 个未闭合左括号的 5 次中位数为 0.000051/0.000076/0.000176/0.002242 秒。
- 状态：审查代理结合扫描器侧验证，R14 针对性关闭。

### R17 / R18：目标唯一性与公开参数校验（主代理修复）

- R17 变更：唯一章号选择，批量/建账在写入前拒绝目标重号，保留不相关重号与原浮点匹配容差。
- R18 变更：前置校验有限章号/范围、枚举、布尔、目录、卷号与补丁来源；模式/平台/范围空格归一；公开入口把受控 I/O 错误转换为 3 与空路径；补齐建账静默行为。
- RED / GREEN：主代理新增 `tests/test_api_input_quality.py` 125 项；在纠正 Windows 临时目录清理夹具后，修改前 `94 failed, 31 passed`；修改后新文件与原 API、历史模块合计 `171 passed`（1.78 秒）。
- 独立复验 / 状态：审查代理 125 项定向测试以及坏输入、跨卷重号、合法归一化和存储异常独立实验均通过，R17/R18 针对性关闭。

### R05 / P2：批量每章重新发现导致 N+1 次扫描（主代理修复）

- 定位 / 根因：批量外层发现一次章节，每次单章管线再次发现完整目录，造成批量 N+1 次重复扫描。
- 变更：`run_audit` 增加内部 `_chapter_snapshot` 可选参数；批量传入本次发现的列表，独立调用继续重新发现，没有全局缓存。
- RED / GREEN：独立 `tests/test_batch_quality.py` 3 项先 `3 failed`，观察到 4/6/4 次发现；修复后连同 R17/R18、原 API、历史与升级模块 `178 passed`（2.64 秒）。
- 同条件性能复测：30/100/200 章均只发现 1 次，发现耗时为 0.0126/0.0392/0.0656 秒，总耗时 0.2592/0.8695/1.7163 秒；原先发现 31/101/201 次，总耗时 0.6058/4.4000/16.2728 秒。每次 N 份归档与 completed 计数均正确。
- 独立复验 / 状态：审查代理验证单次发现、逐章与前章上下文、后续独立调用能发现新文件、批中新增文件不混入已选快照均通过，R05 针对性关闭。

### R16 / P1：第二轨保存失败留下不一致账本

- 定位 / 根因：`save_ledger_state` 在渲染及写入 Markdown 之前提交 JSON；后续失败时 JSON 已改变，旧 Markdown 更早，时间戳检查还可能认为干净。
- 修复计划：先准备两轨内容；双轨保存前保留原字节与恢复信息；任一提交失败恢复已提交目标。若外部锁使恢复也失败，保留恢复副本和明确标记，阻止后续强制复用。单文件保存仍走既有原子写入接口。
- RED 测试：模拟新项目/已有 BOM 与 GB18030 CRLF 文件的第二轨失败、渲染失败，以及外部锁导致回滚失败；断言原字节或可恢复状态，且失败恢复状态不能误认为干净。
- RED：4 项回归均失败，分别证实首次保存残留、已有文件改变、渲染前已写入以及恢复失败仍显示干净。
- 变更：先序列化 JSON 并渲染 Markdown；独占 `.资源账本.json.recovery.json` 标记后保留两轨二进制备份与时间戳，再提交两轨。任一目标写入失败按逆序恢复；首次保存删除本次新增文件和空目录；恢复失败保留标记与未恢复副本。`save`、`sync` 与公共管线加载均检查标记，`force=True` 不能绕过，单文件保存仍走既有原子写接口。
- GREEN：原 4 项通过；补充首次/仅 JSON/仅 Markdown/完整双轨、首轨/次轨失败、旧备份读取失败、mtime 与目录树检查，本轮共 `13 passed`。新旧账本、API、升级、R17/R18 与质量回归共 `331 passed`（6.56 秒）。
- 独立复验：审查代理 13 项定向通过，并用真实 Windows `CreateFileW` 独占句柄在 JSON 更新后阻止回滚；原 JSON 字节与 mtime 留在标记指定副本，dirty=True。双轨/单轨 force 保存、sync、load 与 5 个公开 API 均受控拒绝且残留树不变；按标记恢复后原字节、mtime、目录精确还原，非 force 保存正常且无残留。
- 状态：审查代理针对性关闭 R16。

### R04 / P1：未执行专家仍输出专家成功结论

- 定位 / 根因：运行时仅判断宿主是否可能支持子代理，Python 管线未执行专家，却把规划模式写成实际模式；单章、批量、空继承报告还硬编码资产无冲突、主线平稳、无缝顺承和因果闭合等结论。
- RED：三类运行环境（Codex/generic/subagent）、四种请求模式、单章/批量，以及空继承共 25 项新增回归全部失败。另补平台只有 P3 规则项时不能声称“未命中任何平台规则”的回归，先失败后修正为仅声明未命中 P0/P1。
- 变更：Python 管线与其渲染器均固定 Effective Mode=solo；full/lean/auto 请求追加 `python_api_deterministic_only`，保留原子代理/运行时回退原因。报告和终端增加 `Review Stage: deterministic_precheck`、`Expert Review: not_executed`；bundle meta 增加对应 `review_stage` 与 `expert_review_executed=False`。
- 内容真实性：报告仅陈述已执行的规则发现、账本记录和转场提示词；未执行专家的成功断言改成需宿主语义核验的待办。空继承只说明没有已记录开放条目。原 P0/P1 规则和退出码映射不改。
- 旧测试契约调整：原 API 的 Agent D 标题断言、升级测试的 Effective full/Fallback none 断言固化了本缺陷；首次相关回归为 `2 failed, 33 passed`，按新执行契约更新这两处后通过，其余固定头部键和题材卡尺继续检查。
- GREEN：26 项本轮回归通过；质量回归、原 API、升级、状态机、运行时与批量模块共 `205 passed`（6.22 秒）。
- 持久化分支重开：主代理和审查代理复现平台第一人称 P1 的真实 `category=platform` 与修复建议在单章/批量状态写入中被重建为 `causal` 和通用账本建议，下一批继承继续丢失来源。新增两条公开 API 回归先 `2 failed`。
- 持久化修复：单章直接收集真实 Finding 的类别、建议、位置与证据，批量复用这些问题对象而不再由 P0/P1 字符串列表重造因果记录。显式违规标记仅保留核验原文与设定的事实约束，不假定具体资产问题。
- 第二轮 GREEN：R04 共 28 项新回归与原退出码、跨批继承、去重等针对性检查合计 `35 passed`（1.36 秒）。审查代理此前已验证直接输出的 26 项与 12 个独立模式场景；新增持久化分支已送再次复验。
- 第二轮独立复验 / 状态：审查代理的 R04 28 项定向测试通过；单章/批量分别验证完整 Finding 字段进入状态与下一批继承，真实建议进入报告、重审不重复。结合此前直接输出复验，R04 针对性关闭。

### R13 / P2：正文显式伏笔未接入跨批承诺状态

- 定位 / 根因：无账本项目的显式标签只进入内存；既有账本虽保存 stash，但单章/批量状态写入均未填充 `foreshadowing_commitments`。独立单章调用也未自动加载继承栏目。
- RED：主代理新增 `tests/test_foreshadowing_quality.py` 9 项，初次 `8 failed, 1 passed`；正对照为既有手工承诺与已解决项保留。修复代理补旧字段前置校验、未知旧来源及无显式 origin 的首次来源等 23 项，合计 `31 failed, 1 passed`。
- 变更：新显式标签按 name/origin/status 去重并记录真实 `source_chapter`；将全部 stash（包括先 init 导入的标签）映射至 tag/origin_chapter/status/note 承诺。明示来源章优先，原 origin 文字保存在 note；旧记录确无章号证据时写入 null，报告显示“来源未记录”。
- 持久化与继承：新项目有标签时保存双轨，失败立即返回 3；仅有人工 Markdown 时保留原文件，承诺仍进入审计状态。单章和批量均保存承诺，下一单章或批量调用装载继承；按标签与来源去重，既有手工及已解决记录优先，重复或不再出现都不自动视为回收。
- 兼容保护：新增映射会读取的旧 stash name/origin/status/source_chapter 与承诺 origin_chapter 均先验证，避免给 R01 引入写后异常；未知来源 null 保持合法。
- GREEN：R13 共 `32 passed`；相关质量回归、伏笔、账本、API、升级、状态机、历史、批量与 R17/R18 模块共 `419 passed`（9.45 秒）。
- 独立复验：审查代理 32 项定向测试通过（1.37 秒）；另有 8 个独立场景覆盖有/无账本与两入口继承、去重和手工/已解决保护、先 init 后审后章、自由/未知来源与中文章号、仅 Markdown 保留，以及真实第二轨写失败后返回 3 且无残留。
- 状态：审查代理针对性关闭 R13，17 个代码编号均已针对性通过，最终全量与 R10 文档统计由主代理完成。

### 最终联合冒烟：账本选址夹具纠正（无新增生产缺陷）

- 初始表象：联合调用 init、单章、批量、回写后读取账本的资产为空；章节为 GB18030/CRLF 与 UTF-8 混合，单独提取青钢剑正常。
- 定位：集成夹具只创建正文目录，却硬编码读取 `设定/资源账本.json`。现有 `locate_ledger_paths` 在无设定目录时合法选用根目录 `资源账本.json`；加载不存在的硬编码路径按约定返回空初始状态。
- 逐阶段独立验证：修复代理和审查代理分别在有/无设定目录两种布局执行 init、audit_chapter、audit_scope、apply_fix、sync、checkpoint，全部返回 0；每一步实际定位账本均保留青钢剑 1 柄。修复代理同时验证 GB18030/CRLF 与原字节备份保持；仅错误固定路径在无设定目录时产生空状态。
- 主代理纠正夹具后：六 API 均返回 0，编码/换行、原字节备份、伏笔继承、快照与账本相等检查通过。主代理将以两种布局 × UTF-8/GB18030 四例固化联合回归。
- 处理：未据错误断言修改生产选址或伏笔行为，未新增生产缺陷编号；生产源码保持冻结，等待主代理最终全量检查与提交。

### 最终全量与集成验证（主代理执行）

- 全量测试：`617 passed, 12 subtests passed`，耗时 13.50 秒。共 22 个测试模块，保留原有 252 项测试，新增 365 项。
- 覆盖率：3346 条语句，274 条未覆盖，总覆盖率 92%。
- 静态与兼容性检查：`compileall` 通过；15 个生产模块通过 Python 3.8 AST 语法检查，但未运行 Python 3.8 运行时；生产导入检查未发现非标准库依赖；官方 `quick_validate` 通过。
- 六 API 联合回归：两种账本目录布局 × UTF-8/GB18030 编码，共 4 例通过。
- 真实文本副本冒烟：30 章副本的 init/audit 均返回 0，得到 180 项资产和 30 份单章归档，耗时 0.6439 秒；原章节 SHA256 保持不变。
- 收尾状态：生产源码继续冻结；审查代理收口 R10 文档与最终 diff，提交由主代理执行。此前阶段的 RED/GREEN 结果保留，不以本轮统计覆盖历史证据。
